import 'package:flutter/material.dart';

/// Central place for every brand colour.
/// Put any extra greens you need here later.
class AppColors {
  static const primary     = Color(0xFF2FAC40);   // main brand green
  static const primaryDark = Color(0xFF238233);
  static const lightGreen  = Color(0xFFE6F4E4);
}
